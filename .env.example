# Backend API URL
VITE_BACKEND_URL=http://localhost:8080

# Supabase Configuration
# Obtén estas credenciales desde: https://app.supabase.com/project/_/settings/api
VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
VITE_SUPABASE_ANON_KEY=tu-anon-key-aqui

# Google Cloud Logging (Opcional)
# En Cloud Run, los logs se capturan automáticamente
# No es necesario configurar estas variables, se detectan automáticamente
# VITE_GOOGLE_CLOUD_PROJECT_ID=tu-project-id (opcional, se detecta en Cloud Run)
# VITE_ENABLE_CLOUD_LOGGING=true (default: true en producción)

# Notas:
# 1. Copia este archivo a .env.local y completa con tus credenciales reales
# 2. Nunca subas .env.local al repositorio (ya está en .gitignore)
# 3. El backend debe estar corriendo en VITE_BACKEND_URL para que funcione la app
# 4. Los logs estructurados se envían automáticamente a Google Cloud Logging en producción

